# Based on josean's dotfiles: https://github.com/josean-dev/dev-environment-files/blob/main/.zshrc
export EDITOR=nvim
export VISUAL=nvim

HISTFILE=$HOME/.zhistory
SAVEHIST=1000
HISTSIZE=999
setopt share_history 
setopt hist_expire_dups_first
setopt hist_ignore_dups
setopt hist_verify
setopt NO_BEEP
setopt IGNORE_EOF

# VIM 
bindkey -v
bindkey "^H" backward-delete-char
bindkey "^?" backward-delete-char

# STARSHIP
eval "$(starship init zsh)"

# ZOXIDE
eval "$(zoxide init zsh)"

# FZF
export FZF_DEFAULT_OPTS='--height 40% --tmux --color=bg:#ffffff,fg:#24292f,query:#24292f,bg+:#f6f8fa,fg+:#24292f,hl:#0969da,hl+:#0550ae,info:#57606a,border:#d0d7de,label:#0969da,preview-label:#0969da,pointer:#0969da,prompt:#0969da,spinner:#0969da,header:#57606a,gutter:#ffffff,marker:#1a7f37'
export FZF_CTRL_R_OPTS="--height 40% --tmux --reverse"
source <(fzf --zsh)

# ZSH PLUGINS 
source $(brew --prefix)/share/zsh-autosuggestions/zsh-autosuggestions.zsh
source $(brew --prefix)/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
source $(brew --prefix)/share/zsh-autocomplete/zsh-autocomplete.plugin.zsh

export NVM_DIR="$HOME/.nvm"
[ -s "/opt/homebrew/opt/nvm/nvm.sh" ] && \. "/opt/homebrew/opt/nvm/nvm.sh"  # This loads nvm
[ -s "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm" ] && \. "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm"  # This loads nvm bash_completion


bindkey '^ ' menu-complete
bindkey '^I' autosuggest-accept 

bindkey -M menuselect '^M' .accept-line
bindkey '^[[A' history-beginning-search-backward
bindkey '^[[B' history-beginning-search-forward
export PATH="$HOME/.local/bin:$PATH"

autoload -Uz compinit
compinit

ZSH_CONFIG_DIR="$HOME/Developer/dotfiles/zsh"

# Source custom functions
for file in $ZSH_CONFIG_DIR/functions/*.zsh; do
  source "$file"
done
